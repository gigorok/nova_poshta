require 'spec_helper'

describe NovaPoshta do
  it 'has a version number' do
    expect(NovaPoshta::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
